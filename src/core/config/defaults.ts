import type { StatusItemId } from "./types";

export const DEFAULT_STATUS_ITEMS: readonly StatusItemId[] = [
  "status",
  "agents",
  "model",
  "reasoning",
  "fast",
  "goal",
  "project",
  "queue"
];
