Agent Factory Workspace Kanban Board verification
Work Unit: agent-factory-workspace-kanban-board
Execution invocation: codex-20260726-kanban-board-001
Implementation HEAD: c393790025c70e65a0da0294f419171541857e14

Scope verification
- Canonical Work Unit v4 reader and safe project-root/package boundary implemented.
- Host/Webview boundary accepts only kanban.ready and kanban.refresh from the Webview.
- Six lifecycle columns: backlog, ready, working, review, done, blocked.
- Cards project title, id, updatedAt, execution, Human review, integration, and PR state when present.
- Filter, per-column count, filesystem watcher refresh, reconnect state, empty and partial-error rendering implemented.
- Drag/drop and select/button Move controls consume the same manager transition graph capabilities.
- Drop and Move are preview-only; no kanban.move mutation message exists in this Work Unit.
- No files outside workspace and this Work Unit package were changed.

Commands and results at implementation HEAD
1. npm run check
   PASS: syntax checks and 13/13 unit tests.
2. npm run test:e2e
   PASS: VS Code 1.129.1 Extension Host loaded the development extension,
   projected real canonical Work Units into six columns, rendered Kanban HTML,
   exposed the command, and opened the Webview panel.
3. npm audit --omit=dev
   PASS: found 0 vulnerabilities.
4. Actual canonical reader probe
   PASS: snapshot schemaVersion 1.0.0; backlog 0, ready 0, working 1,
   review 1, done 4, blocked 0; errors [].
5. git diff --check
   PASS.

Recovery history
- The first E2E invocation stopped before extension startup because the fresh
  worktree had no node_modules and could not load @vscode/test-electron.
- npm ci restored 38 lockfile-pinned packages, reported 0 vulnerabilities,
  and both subsequent E2E invocations passed.

Known bounded limitations
- This first Work Unit intentionally does not mutate canonical Work Units.
- Capability targets reflect the current manager transition graph; later
  mutation commands must perform authoritative manager semantic revalidation.
- The E2E verifies canonical projection and generated board HTML inside the
  Extension Host; pointer-level Chromium drag gestures remain Human review.
